# 更新日志

## [2026.08.23-01](changes/2026.08.23-01.md) · 教程逐步引导与客房床型规则

- 教程房间/家具改为每完成一步立即切换下一个具体目标，并同步说明、卡片滚动和高亮。
- 首日口味挑战后自动触发体力归零、回床休息与必流失教学；闭店和王国赠礼增加店主剧情气泡。
- 新增客房床型展示与价格/舒适度规则，限制特殊床混放。
- 接入会议桌专用5×1纹理资产及品质色调分支。
- 强化两侧大按钮闪烁引导，补齐首日第四批离场自动收尾、三种走廊选择、员工招募分步提示、首员自然登场/对话/入座会议、离职消失动画与夜间双击床结束日。
- 本次未执行测试、检查、浏览器或性能验证，未修改 `dist/`。

## [2026.08.21-01] · 夜间交谈与首员入场 UI 收口

- 夜间最后一条“待回应”处理成功后关闭旧交谈弹窗并刷新底栏；仍有队列时仅切换到下一条待处理记录。
- 首员雇用成功后自动显示“新同伴入场”介绍与确认按钮，即使此前教程卡被最小化也不会只显示旧的“继续引导 1/18”。
- 新增两项生产 `UI.onClick` 行为测试；定向 `node --test tests/new-campaign.test.js` 147/147 通过。
- 入场确认按钮改用专用样式类，保持主经营界面单一主操作约束不变。
- 修复 `day1-open` 无可见教学卡时暂停与 1×/2×/4× 倍速被误拦截的问题，第一天其它功能仍继续受门控。
- 固定口味挑战结果关闭后自动显示床教学并隐藏旧版“继续引导”入口，确保玩家能进入 `d1-loss` 教学时序。
- 赠礼确认后自动激活首员招募教学；组合客的接待统计改为每日唯一访客；Day3 两名同门成员的职业与身份字段固定一致。
- 第3日首次免费迁往玄黄界改为带“首次免费迁往玄黄界”标题的约700ms可见转场，结束后自动继续营业。
- 性能短调试覆盖1×与4×；采样保留可见角色/物品min/avg/max，并以平均可见负载门避免单帧裁剪误报，正式20轮尚未启动。
- 性能短调试四行（current/dist各1×、4×）均通过营业负载与错误门；current P95为200.0/202.5ms、dist为187.5/247.4ms，平均可见角色/物品门通过，正式20轮未启动。harness哈希见版本记录。
- 响应式顶栏修复：1280×720/1366×768 下档位与“更多”按钮不再被右侧裁切，并新增真实 Edge DOM 边界检查。
- 修复 v10 legacy 夜间无法结束当天的问题：店主可使用空员工休息室床位，且界面提示不再错误指向玩家休息室。
- 验证收口：legacy 夜床、唯一访客统计、Day3 同门、免费迁界和响应式顶栏回归通过；定向测试 152/152，Edge UI 3/3。
- 性能采样合同改为固定初始化 + 稳定持续负载双层校验，允许两版本真实调度的合理状态演进，避免 4× 采样时逐帧状态差异造成误报。
- 性能短调试通过：current/dist 1× 各 10 秒、初始化与持续负载合同通过、错误为 0；正式 20 轮仍等待冻结哈希与放行。
- 性能采样配对改用 `loadFloor` 布尔合同，避免自然状态演进的非离场精确人数差异造成误报；逐页负载下限仍保持严格检查。
- 性能短调试复核通过，harness SHA-256 为 `9e52c80bbf0adf3b081dc30544bd5631c2a098434dc3c3b53e413a75ea60d346`，正式20轮尚未启动。
- 可见负载改为采样期角色/物品 min/avg/max 统计，使用平均可见数门槛避免单帧裁剪抖动误报，同时保留极值记录。
- 正式性能门已在冻结 harness `a8d35501e9337e5ccc6cb950c4b62c07c77c6f1c16cb93a2998f620189c0faa8` 下完成20/20轮：1×五轮中位 P95 从82.40ms降至47.10ms（改善42.84%），4×从88.20ms降至52.90ms（改善40.02%）；页面、控制台、请求与帧错误均为0，营业负载合同全程通过。

## [2026.08.21-01] · Day1 首批营业闭环补丁

- 店主非直控营业时不再受默认 greeter 岗位限制，可接手备餐、烹饪、出餐、结账与清洁；真实 Sim.update 已验证首批餐饮完成并进入住宿教学，非教学流失为 0。
- 定向 `node --test tests/new-campaign.test.js` 当前 141/141 通过；完整 npm/check/version 校验待本轮收口。
- 教学第1—3日屏蔽常规客人挑战，仅允许第三批 `d1-taste` 两人餐饮客触发固定口味挑战；新增测试后定向 142/142 通过。
- 首日第三批教学容量修复：开门前要求餐厅普通桌具备至少两把有效餐椅，确保两位固定口味挑战客可入座；会议椅不计入。全量 `npm test` 458/458 通过。
- 首员招募教学修复：赠礼后先建短走廊 `corridor2`，再建员工休息室 `lounge5`，最后补会议椅；不放松员工房走廊连通校验。
- 首日床教学入口：首批餐饮结账后显示并高亮玩家床，支持管理点击和直控 E 启动休息；床教学完成后才进入固定流失客时序。
- 修正 `day1-open` 床提示接线，并确保教程客不会叠加普通 challenge；固定口味挑战只生成一次。
- 首员教学走廊蓝图固定为 `corridor2`（2×3、20界币），UI 与 Game 门控不再误选 `corridor3`。
- 床教学改为第三批固定口味挑战确认后出现；`d1-loss` 在休息进行中发生，避免床课提前结束。

## [2026.08.21-01](changes/2026.08.21-01.md) · 新手教学、会议夜间流程与经营稳定性

- 教学存档恢复修复：按真实家具布局自愈缺失初始椅 flag 的半完成教程档，安全处理无下一件家具的恢复渲染，恢复后可进入 `ready-open` 并显示开门入口；新增序列化/恢复回归，`npm test` 450/450、`npm run check` 与版本校验通过。
- 教学餐桌座位修复：会议桌/玩家休息室椅不再冒充餐饮座位；教学顺序、开门前置、存档恢复和 UI 提示统一要求餐厅普通桌与有效餐椅，缺失时自动回到摆家具阶段。新增生产行为测试，`npm test` 452/452、`npm run check` 通过。
- 教学家具面板回归：卡片目标改用餐厅有效座位判定，餐厅椅可点击高亮，会议椅不会错误完成教学；新增真实 Edge DOM 点击验证。
- 教学首日客流修复：第1—3日固定波次不再被随机世界/经营事件打断，住宿脚本不会随机改成先餐后住；首波稳定为单位餐饮客并禁用随机事件弹窗，新增开门推进回归。
- 首日赠礼流程修复：确认赠礼后停在首员招募准备阶段，不再提前打开会议；完成员工房、第二把会议椅、招募和入场确认后才进入会议，且不会重复发放赠礼金额。
- 夜间管理模式修复：店主从会议椅按 E 后会跨帧寻路回玩家休息室床边并弹出结束日确认；补足缺失外观高度时的安全移速计算，避免路径停滞。
- Day1 教学波次修复：下一批客人需等待上一批完成真实服务或到达床位后才生成，避免高速下批次重叠和非预期流失；床教学流失仍由专用脚本控制。

- 重构新档起步和营业结束后的会议/夜间状态，为四日教学和一星认证提供数据基础。
- 修正快速布局重叠与入口、玩家出生点、首椅朝向；补强床铺结算、AI硬超时/取消合并、首日模块门控及 closeBusiness/legacy 差异。
- 补齐四日教学真实阶段、会后事件队列、会议行动点确认、玄黄迁移、会议座位招募锁、内置家具底层保护和管理模式夜床寻路。
- 完成按实际房间/家具校验的四日教学、固定脚本波次、口味挑战与床后必流失体验，并提供快速开局正式入口。
- 增加存档 v11 迁移字段，保留旧档 legacy 行为，并统一记录本次兼容与验证范围。
- 教学专项补强：房间/家具 exact-next 卡片高亮与禁用、低厨艺固定 meal 波次、赠礼后 first-recruitment→首员介绍确认流程，以及带可见转场层的 Day3 异步迁界；`npm test` 299/299、`npm run check` 全绿。
- 教学窄门收尾：首员流程增加会议第二把椅子与完整招聘动作链，画布入口按 exact ghost 拦截越权选择，低厨艺 Day2 教学餐可实际推进至 sleep；新增2项回归测试，`npm test` 301/301。
- 教学状态再收紧：家具阶段允许从正确已建房间切换到 nextFurniture 面板；员工房建成后必须补会议椅，座位足够才开放招聘；新增首员座位链运行态测试，`npm test` 302/302。
- 修复房间/家具教学完成后的 phase 覆盖，并将首员入场确认统一到 `Sim.confirmEmployeeIntro()`；补充会议椅状态回归。
- 新增 `tutorial-actions.js` 共享教学控制器，统一 Game 与测试的建房、家具放置、选房和 phase 推进；新增真实 table 选房/放置链回归。
- 生命周期 closing/report 子包：结算后先播放打烊标题和集合阶段，会议座位分配完成后才展示日报；报告与 post-report 阶段保存状态，closeBusiness 和 finishNight 保持幂等，日报 AI 失败自动本地回退。
- closing P0 修复：员工通过真实 path 前往会议椅后才落座，保存恢复 `lastStat/reportState`，并增加统一 `resumeCampaignPhase()`；legacy 无座位继续兼容。
- 认证、邀请和比赛按钮接入共享 post-report controller，事件状态/游标/reportToken 持久化推进；新增生命周期开门 phase 表回归。
- closing 中途存档恢复现在通过共享 `resumeClosingPhase()` 重建座位路径，恢复后继续逐帧集合且不会重复打开日报或结算。
- post-report controller 现在按当前事件类型校验 certification、邀请、比赛和 finale 动作，统一完成并推进队列；closing timer 统一使用 `closingState`。
- 增加 `drivePostReportEvents()` 唯一事件驱动器：认证庆典回到日报后才消费队列，邀请/比赛/finale 使用专用 UI 与严格 controller，未知事件才走通用确认。
- 性能预审失败修复（仅 fixture/合同）：统一固定 v10 fixture canonical SHA-256 `f7913e461e250cfda9bc3ad8f038c4aabf37fc4a71c6670095e203ecf0be9972`，补齐客人运行态有限字段并消除真实推进 NaN；采样合同允许两版本自然演化但继续验证营业态、固定规模、有限值、非全离场和有效任务，setup 直方图保持严格固定。current/dist 各完成预热1秒+采样10秒 GPU 短调试，NVIDIA RTX 2070 D3D11、约56–61 FPS、无页面错误；`npm test` 446/446、`npm run check` 通过。正式20轮等待再次放行。
- 最新短调试 artifact：current 581 frames/58.1 FPS、RAF P95 82.0ms/P99 152.9ms/max 294.1ms；dist 507 frames/50.7 FPS、P95 88.2ms/P99 135.4ms/max 282.4ms；两页错误均为0，采样10秒。单轮短样本不代表正式性能门，20轮仍等待再次放行。
- 性能合同唯一阻断修复：采样快照补齐完整客群状态、任务总数与排序任务种类；允许两版本员工任务分配存在单项差异，但严格比较总量/种类/完整状态。持续负载要求非离场客至少8、任务至少6。语法、性能定向3/3及两URL各10秒 debug 通过，错误均为0；正式20轮等待 Sol 放行。
- benchmark 镜头与清理收尾：短调试固定 zoom=1、中心 `(10,3)` 覆盖中央餐饮区；harness 结束时显式清理浏览器与 HTTP 子进程并保留可见负载/环境证据，不改生产源码或 dist。正式20轮仍独立执行。
- benchmark cleanup 验证：单次 `PERF_DEBUG=1` 明确输出 `EXIT=0`；current/dist 各10秒合同通过，current 25 actors/4 items、P95 76.5ms，dist 30 actors/24 items、P95 82.4ms，错误为0；复查无 benchmark Node 或 4173 服务残留。harness SHA-256 `35cd515458384c4d4597391866e4d823dbd704bc92fb8f289bce4e7d6ee48632`。
- cleanup 最终修订：`closeBrowser` 使用 active browser 的有界 close，不调用 browser process API；短调试明确 `EXIT=0`，current/dist 合同与错误检查通过，无 headless Edge、benchmark Node 或4173服务残留。harness SHA-256 `933dd1faf08d851888cedc7fcf50ab28f0fc0b75cc8ed8bdf6fb26a8ad935863`。
- 教程 UI P0：建房教学目标卡按房间 kind 正确启用位面门厅，首员流程仍按具体蓝图 id，非目标卡继续禁用；Game 方法层门控保持严格。新增生产判定测试，UI 定向 40/40；node 语法检查通过。npm wrapper 因 C 盘 ENOSPC 未完成。
- 教学家具完成 P0：完成判定读取真实 Tavern 家具，初始玩家休息室椅子不再因缺失历史 `furn_chair` flag 被漏算；教学家具齐备后进入 `ready-open`，开门方法和顶部入口恢复。新增标准新档 Edge 目标卡回归；全量 `npm test` 449/449、`npm run check` 与版本校验通过。
- 教学存档恢复 P0：教学目标卡在 resume/render 后继续保留高亮与可点击状态；真实家具齐备的半完成教程档不再因 `nextFurniture=undefined` 调用 `furnDef` 崩溃，恢复后可进入 `ready-open`。标准新档及恢复回归纳入全量测试。
- 夜间/床/天明闭环：新增员工主动交谈专用额度与稍后队列、夜间自主活动次数边界和存档；普通聊天不能绕过额度；手动店主无输入不再清除床任务；天明加入可观察遮幕、中央“休息结束 经营时段”文案、输入锁和幂等恢复。
- 本次功能包验证：`tests/new-campaign.test.js` 39/39；完整 `npm test` 与 `npm run check` 结果记录于发布前验证。
- 夜间 P0 修订：员工主动事件进入 tick 寻路与专用额度，店主主动交谈不再修改员工主动记录；休息计入自主次数；床交互绑定具体床位并支持 E/管理取消；天明 active 纳入全局输入阻断。
- 补齐夜间底栏待回应入口、主动候选 tick 调度、天明恢复入口及指定床位生产测试；定向 `new-campaign` 测试提升至 41/41。
- 二次夜间 P0 修正：区分两类交谈额度与指定队列处理，过滤活动中同事，限制店主床位至玩家休息室，并为天明状态补充 token/stage。
- 新增 ready dispatcher 与同床 cancelRest 生产行为测试；全量测试 330/330 通过。
- dawn production 收尾：天明启动立即保存 active/stage/token，过渡期间锁定 Game 输入与普通模拟更新，存档恢复后只完成一次 finishNight；测试总数 331/331。
- proactive P0 收尾：ready 队列改为 FIFO、多员工同帧不丢失，dispatcher 逐一弹窗处理并保存；员工详情不再绕过 dispatcher 触发主动事件。
- proactive save/load 收尾：增加 inFlight 原子状态与旧档兼容，保存/载入不会重复消耗或丢失当前主动员工。
- 空间兼容补强：统一房间连通根规则，legacy 使用 foyer，新 campaign 使用 playerroom，并覆盖拆除连通性回归。
- 性能基准补齐：固定 v10 legacy fixture 可由 current/dist 两版实际加载并核对 35 房、250 家具、12 员工、30 客；GPU Edge 下 1×/4×各5轮、每轮预热5秒采样10秒，页面错误0，完整结果与硬件/源码哈希写入 `artifacts/performance/browser-raw.json`。current 的固定场景 P95 暂未达到30%改善门槛，已如实记录。
- 性能子包 B：固定 fixture 现在经生产开门路径进入营业态，包含真实客流、桌椅与基础厨房/储藏/客房设施；新增 runtime counts、task rebuild、frame P95/长帧和 UI panel rebuild instrumentation，并将 toast/结构面板改为脏签名更新。结果仍以 active browser artifact 为准，未宣称达到30%改善门槛。
- 性能子包 B 正式基准达到门槛：current 相比未改动 dist 在固定 active business fixture 下 1× P95 改善约84.4%、4×约85.4%，20轮页面错误0；runtime counts、任务重建、frame P95、panel rebuild 和 GPU renderer 记录于 `artifacts/performance/browser-raw.json`。
- 空间底层校验：新增统一 `validateLayout` 生产 API，按门洞图验证根房间、布局重叠、家具合法性和内置绑定，并补充恶意布局行为测试。
- validator 补强：增加房间/家具字段与 footprint、内置家具契约和严格根校验；Game v11 存档读取采用 clone-first strict load，非法布局在替换前拒绝。
- 性能门修订：扩展动态面板签名、0.5 秒安全重扫行为验证、无碰撞污渍签名和统一视口裁剪；固定 fixture 生成 canonical SHA 链并增强浏览器基准错误、环境、源码哈希和 AB/BA 记录。`npm test` 443/443、`npm run check` 通过；正式20轮基准待 Sol 放行。
- 性能预审修订：修正 overlay 像素坐标裁剪、补充建造/移动/拆除/任务完成与失败失效事件、补充员工好感签名；fixture 补齐有限评分字段并更新 canonical SHA `e85e2bd91438e68c9fe580b5e28283164c263e80957b5f3ba1f36b3d85f971fd`。current/dist 短调试均核对 35/250/12/30 且无 NaN；全量测试 443/443、语法检查通过，正式20轮待放行。
- 性能预审第二轮：扩展员工/客人/设施/FX 全部视口裁剪，修正 fixture 客组与订单/设施冲突，增加 current/dist 运行态和任务直方图等价校验及浏览器真实版本、UA、DPR、visualViewport scale/zoom 记录。短调试两版均有限值、无 frame/page/console/request 错误；`npm test` 443/443、`npm run check` 通过，正式20轮待放行。
- 性能基准异常路径增加 browser/server 资源回收与非零错误门，禁止缺失 P95 进入汇总。
- 性能预审第三轮：加入 setup/sample captureContract 与 current/dist 归一快照等价校验，固定客群/任务直方图和持续负载门；新增真实 UI render(false) 动态字段重建测试及共享屏内/屏外裁剪 predicate 测试。`npm test` 446/446、`npm run check` 通过。
- validator 窄验补强：加入自增游标安全校验、legacy 结构安全检查和拆房 clone 校验，失败不会修改原布局。
- roots 窄验：连通图与拆房核心根显式接受 mode，tutorial 只使用 playerroom，legacy 只使用 foyer；Game 生产调用传递 campaign mode。
- roots/load/remove 精确修正：删除旧无参 root 方法，v10 provenance 决定 trusted legacy，Game load 始终结构 strict，拆房 clone 移除房内全部家具后再验证。
- UI 行为补测：卧室姓名标签统一使用 `renderPersonName`；真实营业 tick 验证低体力员工面对迎宾积压时至少一人接单而非集体休息。定向 UI 测试 35/35 全绿，性能基准和最终浏览器验收仍待执行。
- UI 最终 P1：聊天历史、关系、能力/状态说明、夜间队列、房间住户与驻守姓名统一显示性别标记；职责优先级改由共享 `handlePriorityUiAction` 驱动并新增真实 setter 行为测试。定向 UI 测试 37/37，性能基准和浏览器验收仍待执行。
- UI 姓名视图收尾：进修计划/归来反思和店主身份页统一使用 `renderPersonName`；定向 UI 测试 38/38。
- 性能专项第一阶段：任务计划事件失效与 0.5 秒安全重扫、房间/家具索引、LRU 路径缓存、角色/物品视口裁剪和性能计数入口已加入；固定 fixture 与 baseline/current 原始 JSON 已保存，但当前 P95 尚未达到 30% 改善门槛，仍需继续优化。
- 房间建造/移动 production 接线：扣款前验证自动内置家具与门洞布局；房间整体移动使用 clone 同步房内家具和旋转，断网、重叠或非法邻接均原子拒绝；新增 9 项布局行为测试，定向 76/76、全量 363/363 全绿，check/version 校验通过。
- 房间拆除/家具移动 production 接线：拆房和家具拖动、旋转均先验证 clone 后提交；拆房清理房内家具、污渍和任务，退款排除内置家具并读取 purchasePrice；新增 8 项行为测试，定向 84/84、全量 371/371 全绿。
- 步骤 3a P0 收尾：Tavern 存档改为深拷贝快照；休息室直连走廊约束接入建造/移动；房间蓝图排除内置家具并保留普通家具字段；房间记录实际建造价，拆除退款按 purchasePrice 计算；新增 8 项测试，定向 92/92、全量 379/379 全绿。
- 步骤 3a 最终 P0：拆房按实际房间 purchasePrice 退款；蓝图读取归一旧内置家具并过滤自动件；复制房间统一验证“新房+自动内置+普通模板”完整 candidate 后才提交；家具建造保存实际支付价；新增 4 项测试，定向 96/96、全量 383/383 全绿。
- 步骤 3b 批量替换/恢复 production：整店布局、撤销/重做、开局布局、存档 load 和 openDay 均接入 clone-first trusted validator 与原子提交；失败恢复旧状态，legacy/tutorial 根模式不可互换；新增 10 项行为测试，定向 106/106、全量 393/393 全绿。
- 步骤 3b 最终 P0：主存档/备份/导入/恢复统一通过 parseValidateSave strict 校验，坏主档只回退合法备份，双坏不覆盖当前状态；newTavern 失败回滚。新增 6 项测试，定向 112/112、全量 399/399 全绿。
- 步骤 3b storage 最终 P0：新增 `src/save-validation.js` 统一存档 string/object 解析、迁移、seal/trusted mode 和 strict load；所有存档入口复用，备份恢复原子写回且不改当前运行态；新增 8 项测试，定向 120/120、全量 407/407 全绿。
- storage 最终入口 P0：Game loadFrom 复用 validated result，saveToSlot 成功后才切换目标槽，备份回退具备写入/加载失败回滚；新增入口原子性测试，node 全量 411/411。
- roots/trusted mode 最终窄验：默认 mode 不从房间内容推断，Game 依据原始版本判定 legacy，v11 伪造字段无法切根；新增6项根、严格载入与拆房回归。
- legacy seal：旧档迁移生成绑定布局 canonical seal，v11 重新载入须同时满足 legacy campaign 与 seal 验证；seal 只决定兼容模式，不放松结构校验。
- legacy runtime 洗白修正：加载后强制同步 trusted campaign/Tavern 状态，保存仅依据已验证 runtime 生成 seal，原始伪造字段不能被重新签名。
- validator 完整契约：补齐蓝图尺寸、旋转 footprint、家具字段/重叠和内置家具契约校验，legacy 只跳过新内置元数据要求。
- validator 契约窄验：tutorial 严格匹配 blueprint ID/kind/旋转尺寸，强制正安全 ID 与数组容器，封闭内置家具契约并覆盖 dining 伪造内置测试。
- 生产接线补充：新增 clone 候选布局校验 API，tutorial 新档开业前执行当前布局 validator，旧 legacy fixture 保持兼容；全量 354/354。
- 日常 mutation 接线补充：家具删除/旋转使用 clone-first validator，非法变更不会污染布局、退款或选择状态。
- 家具建造 production 接线：放置家具先验证 candidate，再进入购买确认和扣款，失败保持金币与布局不变。
- 房间建造 production 接线：新房与自动内置家具先组成 candidate 并验证门洞/根/走廊规则，再扣款提交；非法位置保持原布局和金币。
- post-report P0 收尾：庆典结束保留日报 stat，日报确认后才进入逐项事件；五星终局使用 `finalenext` 专用确认，赛事跨日和五星认证事件按当日报告 token 幂等入队，赛事胜利不再错误触发五星。新增4项行为测试；定向25/25、全量312/312、语法检查全绿。
- 会议/夜间/营业床/天明子包：加入稳定三类会议议题、员工态度与发言、夜间两次自主行动及双向交谈额度、床边营业休息结算、床边结束日和可见天明转场；AI 聊天/夜间失败自动本地回退并拒绝未知会议议题。新增行为测试，定向31/31、全量318/318、语法检查全绿。
- 会议 production 收尾：动态议题按日报/员工/世界驻留确定性生成，会议控制器统一提前散会、效果幂等、save/load 与 AI ID 校验；UI 展示员工态度及本地/AI 发言，失败自动本地回退。新增会议行为测试，定向34/34、全量321/321；最终 npm 验证使用 G 盘缓存完成。
- 会议 modal P0 修复：主会议无 X 且 ESC 不可旁路关闭；提前散会使用专用确认/返回会议 action，返回保持 cards、AP 和选择，确认后才进入夜间；选卡统一走 meeting controller。新增真实 UI handler 回归，定向35/35、全量322/322。
- quiet/corridor 子包：房间噪声/安静度接入客房舒适与报告分项，员工房不读取 quiet；走廊维护费按40%、污渍概率按25%缩放，路径/移速规则回归；全量 node 测试 419/419。
- 走廊污渍运行态修正：`tickWorld` 先按真实活动房间选点，再应用房间污渍概率；生产测试确认走廊 `0.00045`、普通房 `0.0018`，全量 node 测试 420/420。
- UI/legacy 回归包：捏人属性控件防误提交，角色姓名旁统一性别小字，内置家具从普通目录隐藏并禁用复制/升级/出售，招聘提前显示员工房/会议座位限制，解雇即时清理任务与夜间队列，legacy 世界按星级解锁；全量 npm 测试 426/426。
- 浏览器响应式验收暂未完成：Playwright Chromium 下载因 C 盘 ENOSPC 中止，未伪造 1280×720/1366×768 截图证据。
- UI 包 P0/P1 收尾：解雇工作安全释放与订单续接、legacy desk/bunk 补购、招聘门控、统一姓名性别渲染和积压任务待命逻辑；全量 npm 测试 432/432。
- UI 包最终 P0：订单取餐标志幂等释放、全视图统一姓名性别渲染、捏人生产事件 dispatcher、优先级任务匹配与紧急积压行为回归；全量 npm 测试 434/434。

## [2026.08.20-01](changes/2026.08.20-01.md) · 中期性能与员工操作修复

- 优化中期营业性能，修复店主创建、AI JSON、自定义优先级、解雇员工和窄屏顶栏问题。
- 详细改动与验证结果见版本记录。
