# 2026.08.21-01 · 新手教学、会议夜间流程与经营稳定性

## 功能

- 新档改为玩家休息室起步，补充会议桌、座位容量、门厅和员工休息室床位规则。
- 将闭店流程拆分为经营结算、日报、会议、夜间自由活动和睡眠后晨间准备。
- 增加会议议题状态、夜间员工互动额度、日期解锁和连续驻留状态的数据结构。
- 为第1—4天教学、首日赠金和一星分档奖励预留幂等状态。

## 修复与性能

- 修复旧存档迁移的兼容入口，保留 v10 legacy 布局。
- 统一走廊、安静度、会议座位和床铺休息配置。
- 改善 AI JSON 解析错误信息并保留本地回退路径。

## 兼容性与验证

- 版本号升级至 v11；旧存档不会强制插入玩家休息室。
- `npm run check` 已通过；全量 `npm test` 295/295 通过（含快速开局无重叠/入口、会议夜间、床铺边界、v10 legacy、路线解锁和会后队列集成测试）。
- 浏览器冒烟已完成：1280×720 可进入档位选择、店主创建和固定玩家休息室页面；1366×768 首页加载正常，控制台无错误。
- 已补充：第1天银冠王国赠金状态、首日会议议题、夜间员工互动额度、床边结束日入口、世界日期解锁和连续驻留耐心衰减。
- A* 开放集改为二叉堆，走廊路径权重与移动速度规则已接入；角色在走廊移动速度提高25%。
- 路径基准（35房间布局、2000次重复路径查询）P50 0.0006ms、P95 0.0008ms、最大0.526ms；尚未完成真实250家具/12员工/30客人的完整渲染基准。
- 教程第一天招聘入口已在 UI 与事件处理层同时锁定；快速开局入口使用推荐布局后直接进入第2天准备阶段。
- 第二轮浏览器实测在 1280×720 与 1366×768 均完成“开始新游戏→档位1→店主→酒馆设定”，固定休息室显示、布局选择按钮为0、页面错误为0。
- 门厅蓝图界面显示实际建造价50；快速推荐布局调整为合法不重叠的房间拓扑，总价仍为2880，客人入口固定取门厅，店主出生固定取玩家休息室。
- 内置会议桌首椅固定在休息室局部(1,4)并朝右；玩家休息室家具和门厅入口不再混用。
- AI 请求现在合并调用方取消信号与15秒硬超时；营业床完成后体力设为100、压力降低20且不改变士气，中断不结算。
- v11 新档的 closeBusiness 不再在日报阶段伪造恢复、亲密度或辞职副作用；legacy 旧档保留兼容关系结算。
- 第一日 UI 隐藏菜单/经营导轨并在事件层拒绝访问，固定教学客人身份、预算、口味和耐心快照；常规新档客源固定当前驻留世界。
- 全量回归最终结果：`npm run check` 通过，`npm test` 292/292 通过；规定的完整250家具/12员工/30客浏览器性能对比仍未完成，不能视为发布验收通过。
- 功能闭环补充：教程阶段按真实建房/家具/开门状态推进并使用暗幕高亮；首日 UI 与 GameApi 双重隐藏成长、关系、岗位、区域和职责设置。
- 第三天晨间迁移改为一次性免费玄黄流程；普通客源仅当前世界，教学异界客显式固定；路线解锁校正为1/3/6/9…33天。
- 会议剩余行动点需确认才能提前散会；会后赠礼、认证和赛事邀请进入幂等 postReportEvents 队列并在日报后消费。
- 会议桌座位成为非 legacy 招募/开门硬锁，最多12名员工；内置家具底层删除、复制和直接建造均拒绝，员工休息室建造自动附带绑定床。
- 夜间管理模式会将店主寻路至床边，直控模式必须在床边交互；离职会即时释放任务。服务端 AI 代理与客户端统一15秒超时。
- 本工作包验证为 `npm test` 295/295、`npm run check` 全绿；性能基准与最终浏览器长流程仍待后续验收。
- 最后一次教程遮罩隐藏状态回归后重新执行语法检查与全量测试，结果保持 295/295。
- 教学与四日战役包：新 campaign 教程不再使用旧17步导览，按每一间实际存在的房间/家具逐步高亮；遮罩事件层仅放行当前教学动作和必要地图落位。
- 0星教学客房临时开放；拆除房间或家具后开业会重新以真实地图状态校验，不接受旧 flag 冒充完成。

## 夜间、床铺与天明补充

- 夜间主动交谈现在通过专用额度处理：每晚最多三名员工主动找店主，每名员工一次；立即、稍后队列和婉拒均可保存恢复，店主主动交谈额度独立，普通 `chatWith` 不再绕过夜间规则。
- 夜间自主活动只在动作完成后扣除次数；同事聊天要求双方仍有次数，取消/寻路失败不扣除，次数耗尽后员工返回所属休息室床位。
- 修正直控店主无输入时不会静默清除床休息任务；明确移动输入才取消任务。营业床继续按品质 I/II/III 使用15/10/5秒，完成体力回满、压力降低20，中断零收益。
- 天明流程新增真实遮幕层与 start/middle/end 可观察阶段，显示“休息结束 经营时段”并锁定输入；过渡完成后才恢复 prepare、推进日期，重复触发幂等。
- 新增夜间主动额度边界、UI稍后队列、手动床任务和天明过渡行为测试；定向 `tests/new-campaign.test.js` 为39/39。
- 最终本地回归：`npm test` 326/326、`npm run check` 全部通过；完整浏览器长流程与性能基准仍由发布验收阶段执行。
- 夜间 P0 修订：员工主动候选在夜间 tick 中实际寻路至店主，店主主动交谈独立写入 `playerTalks`；休息动作也消耗一次自主额度；营业床请求绑定具体 bedId，直控 E 可开始/取消，管理路径不再随机换床；天明 active 状态纳入 Game blocked 输入锁。
- 本轮补齐夜间底栏“待回应 N”入口、主动候选 tick 寻路、天明存档恢复与指定床行为测试；定向测试 41/41。
- 二次 P0 修正：夜间 owner→staff 交谈统一写入独立 `playerTalks`，待回应队列按指定员工处理；同事候选排除活动中角色；店主营业床仅允许玩家休息室，天明状态增加 token/stage。
- 本轮新增 production ready dispatcher、指定床 cancelRest 运行态测试；定向 43/43，全量 `npm test` 330/330，`npm run check` 通过。
- dawn production 收尾：开始天明前同步写入 active/stage/token 并保存；dawn active 时 Game frame、方向和快捷键全部锁定；存档恢复可重建过渡并只推进一次日期。新增测试后定向 44/44、全量 `npm test` 331/331、`npm run check` 通过。
- proactive P0 收尾：ready 改为 FIFO 队列，beginNight 不预填 ready；dispatcher 逐员冻结、处理后解冻，活动员工不再分配夜间动作；详情页移除员工主动按钮。定向测试 45/45。
- proactive save/load 收尾：新增 `proactiveInFlight` 原子状态，弹窗打开前保存；载入后按同一员工恢复弹窗，决定完成才清理冻结和 inFlight；旧档 ready 字段兼容迁移。新增测试，定向 46/46。
- 空间/legacy 兼容补强：房间连通校验根据 legacy 选择 foyer 根、新 campaign 选择 playerroom 根；拆除/移动连通性继续复用统一根规则。新增空间根回归测试。
- 空间/导入底层子包：新增基于真实门洞图的 `validateLayout`，校验根房间、重叠、家具落位和内置绑定；Tavern 暴露统一 validator，并覆盖合法 quick 布局、缺根和重叠恶意布局。
- validator 子包补强：严格检查房间/家具 ID、类型、坐标、占用 footprint、campaign 根与内置家具契约；`Tavern.load(data,{mode,strict})` 克隆校验后再载入，正式 v11 Game 读取接入 strict，失败不污染当前布局。
- 新增 strict load 恶意重复房间 ID 行为测试；本轮定向测试 49/49。
- validator 窄验补强：校验 nr/nf 安全游标，legacy 跳过仅限新内置契约而仍保留结构安全检查；canRemoveRoom 对移除后 clone 重新执行统一校验，并补充失败原子性测试。
- validator roots 窄验：新增显式 `rootRoom(rooms, mode)`，tutorial 只认 playerroom、legacy 只认 foyer；canRemove 核心房间规则按可信 mode 判断，Game 调用传入 campaign mode。
- 本轮精确修正：移除旧无参 root fallback；v10 migration 写入 `migrationProvenance`，Game load 始终 strict structural 校验；canRemove clone 按 `roomOfFurn` 过滤房内全部家具。全量测试 337/337，check 通过。
- roots/trusted mode 最终窄验：默认连通 mode 仅读取 `this.legacy`，Game load 依据原始 sourceVersion≤10 判定 trusted legacy，v11 伪造 provenance/legacy 字段不能切换根；Tavern.load mode 覆盖 payload legacy。新增6项生产行为测试。
- 兼容旧构造：首个 foyer 房间布局自动标记 legacy，非 legacy 显式状态仍严格只认 playerroom；本轮全量回归继续执行。
- legacy seal：v10 migration 生成绑定布局 canonical 内容与版本盐的双 hash seal；v11 legacy reload 仅在 campaign.mode=legacy 且 seal 校验通过时启用兼容根规则，普通 legacy 布尔字段或伪造 provenance 无法切换。新增 seal 绑定测试。
- legacy runtime 洗白修正：Game load 计算 trusted mode 后强制覆盖 Sim campaign 与 Tavern legacy 状态；保存 seal 只读取已验证 runtime，不读取原始 payload 字段。新增伪造 v11 runtime 回归。
- validator 完整契约：严格校验房间蓝图/尺寸、家具旋转 footprint、ID/坐标/品质、内置 bed/table/chair/desk/bunk 契约和普通家具重叠；legacy 仅 grandfather 新内置契约。新增契约恶意布局回归。
- validator 契约窄验：tutorial 强制 bp ID/kind/旋转尺寸一致，房间/家具 ID 必须为正安全整数，rooms/furns 非数组受控失败；封闭内置契约拒绝 dining 伪造内置椅或错误价格。新增8项恶意布局测试。
- 生产接线补充：新增 `validateCandidateOrError` 原子候选校验 API，并将 tutorial 新档 openDay 当前布局 readiness 接入 validator；兼容旧 legacy fixture 不改变既有流程。全量测试 354/354。
- 日常 mutation 接线补充：家具删除与旋转在提交前对 clone candidate 执行完整 validator，失败不改原家具、退款或选择；openDay readiness 已接入新档布局校验。全量测试 354/354。
- 家具建造 production 接线：tryBuildFurn 在确认扣款前构造完整 candidate 并执行 validator，非法 footprint/契约不会扣币或提交。全量测试 354/354。
- 房间建造 production 接线：tryBuildRoom 在扣款前构造新房及自动内置家具 candidate，校验 trusted mode、门洞连通和休息室走廊要求；失败不扣币、不改布局。全量测试 354/354。
- Day1/2/3 波次增加稳定脚本 ID、成员身份/职业/年龄/性别/预算/口味/续住需求；Day2 本地客明确 meal→sleep，Day3 异界客明确指定。
- Day1 口味挑战进入真实 pendingEvent 并可消费；完成玩家床教学后才生成必流失波次，流失计入日报 lost。
- 首日赠礼后建员工休息室才解锁招聘，首员入职立即解锁成长/关系/岗位功能；快速开局在正式酒馆设置页提供可操作选择。
- 本包新增行为测试并将全量测试目标更新为 298/298；仍未进行性能基准和最终浏览器长流程验收。
- 教程最小化/跳过按钮在每个战役步骤保持可用，其他面板动作继续按阶段拒绝。
- 教学专项追加严格 exact-next 房间/家具动作校验，禁止越序建造或未齐房先摆家具；教程客脚本固定所有玩法/叙事字段，仅保留名称与外观随机。
- Day1 床任务开始即记录教学时序，低耐心客在15秒床休息期间出现并耗尽耐心，完成床任务前即可计入流失；Day2续住需求保持 meal→sleep。
- 首日赠礼事件消费后进入 first-recruitment 专用阶段，只允许建员工休息室；建成后解锁招募，首员入职仍保持第1天。
- Day3迁移通过异步 Promise/可注入动画回调跨帧完成，完成前保持旧世界与 transition，完成后才进入 prepare。
- 专项集成测试后全量结果更新为 299/299；仍未进行性能基准和最终浏览器长流程验收。
- 最后补充严格 phase exact-next 方法层回归：建房阶段拒绝家具，家具阶段拒绝房间，首日 recruitment 阶段只接受员工休息室；`npm run check` 与 `npm test` 299/299 保持通过。
- 教学 UI 现在为房间蓝图和家具卡输出稳定 `data-tutorial-type/key`；非当前 exact-next 目标以 `aria-disabled` 禁用，暗幕高亮会随面板打开切换到具体卡片。
- 教学客在生成时强制固定 meal 需求及完整玩法/叙事字段，低厨艺也不会因可用需求降级而丢失；随机标志人物字段会被清理，保留名称与外观作为唯一随机项。
- 首日赠礼消费停留在 `first-recruitment`，可退出弹窗后建员工房、招募首员，再确认入场介绍才进入会议；Day3 教学迁移使用可见 world travel layer，动画回调完成后才激活玄黄并进入 prepare。
- 本次增量验证：`npm run check` 通过，`npm test` 299/299 全绿；性能基准与最终浏览器长流程仍未执行。
- 教学窄门收尾：first-recruitment 以员工休息室 blueprint id 精确高亮，完整放行广告/候选比较/雇用动作链，并要求先在会议桌增加第二把椅子；首员入职仍需确认入场介绍。
- `game.click` 在房间/家具教学阶段拦截未选 ghost 的角色、旧房间和地图选择；低厨艺 Day2 教学客获得固定基础餐保底，可从点单推进到结账后的 sleep 需求。
- 新增2项行为回归测试；全量验证更新为 `npm run check` 通过、`npm test` 301/301 全绿。性能基准与最终浏览器长流程仍未执行。
- 教学最终窄门：无家具 ghost 时可点击且仅可点击容纳 nextFurniture 的已建房间切换家具面板；员工房建成后保持 `first-recruitment`，进入会议椅准备子流程，座位达到2席后才转 `recruit`，并修正使用 `Sim.meetingSeatCapacity()`。
- 新增首员座位→招聘→employee-intro 运行态测试；全量验证目标更新为 `npm run check` 通过、`npm test` 302/302 全绿。
- 修复教程阶段覆盖：房间完成后只有从 `tutorial-build` 进入才会切换 `tutorial-furnish`，家具完成后只有从 `tutorial-furnish` 进入才会切换 `ready-open`；首员确认统一使用 `Sim.confirmEmployeeIntro()`。
- 增加会议椅与首员入场确认运行态回归；全量测试目标更新为 `npm test` 303/303，性能基准与最终浏览器长流程仍未执行。
- 提取共享教学控制器 `src/tutorial-actions.js`，Game 与测试共同调用房间/家具 phase 推进及家具阶段选房；新增真实共享路径测试覆盖员工房→会议椅→招聘和 guestroom→dining→table。
- 定向测试 `node --test tests/new-campaign.test.js` 现为16/16全绿；完整回归目标更新为 `npm test` 304/304。
- 生命周期/会议/夜间增量：结算幂等、post-report drain、AI 日报失败自动本地回退、夜间床结束入口改为床边确认且移除全局结束按钮；会议座位招聘阶段保持 first-recruitment。
- 新增端到端营业结算→事件队列→会议→夜间→天明幂等测试；定向 `tests/new-campaign.test.js` 17/17 全绿，完整回归待执行。
- closing/report 子包：finishDay 不再同栈打开日报，进入 closing-title→closing-assemble，集合并分配会议座位后才进入 report；日报队列阶段持久化 reportState，closeBusiness 同日幂等，AI 日报失败自动本地回退。
- 增加会议桌座位分配与存档字段、夜间床边结束保护及跨阶段幂等回归；`npm run check` 通过，`npm test` 304/304 全绿。
- closing P0 修复：集合阶段不再瞬移，员工保存会议座位目标/path，update 到达后才坐下并进入 report；legacy 仍允许无座位降级。Sim serialize/load 现在恢复 `lastStat/reportState`，Game 提供 `resumeCampaignPhase()`。
- 新增真实打烊寻路回归；定向 `tests/new-campaign.test.js` 18/18 全绿。
- closing P0 二次修复：Game frame 在 closing assemble 阶段即使 blocked 也推进寻路，移除2.2秒强制开日报；删除 pendingSettlementStat/contestAfterStat 旁路，认证/邀请/比赛继续从 reportState 取日报。
- UI 开门按钮与 Game.openDay 统一调用 `Sim.canOpenBusinessNow()`，生命周期中间阶段全部拒绝开门；全量测试保持通过。
- 提取 `closing-controller.js` 供 Game frame 与行为测试共同驱动；closing 阶段 blocked 仍移动，2.2 秒不会提前 report。post-report 事件统一增加 `status/reportToken/cursor`，UI 读取当前未完成事件。
- 定向测试更新为19/19；完整回归 `npm test` 保持304项全绿。
- 新增共享 `post-report-controller.js`，认证/邀请/比赛动作统一从持久事件队列和 reportToken 推进；UI 的邀请接受/拒绝和比赛结果改调用共享 controller。
- 新增 `canOpenBusinessNow()` 生命周期 phase 表行为测试；定向 `tests/new-campaign.test.js` 20/20 全绿。
- closing 最后收尾：提取 `resumeClosingPhase(sim)`，Game 恢复和 Node 行为测试共同重建座位 path；中途 serialize/load 后生产 closing controller 逐帧到座，仅触发一次 report，coins/stat/reportToken 保持不变。
- 定向测试更新为21/21全绿；完整回归随后执行。
- post-report timer 收尾：certification/invite/active-contest/finale controller 方法校验当前事件 kind；确认后统一标 done 并推进下一项，队列空才进入 meeting。Game 使用统一 `closingState` 计时字段。
- post-report 唯一驱动器：`drivePostReportEvents()` 按 current.kind 打开认证/邀请/比赛/finale 专用 UI，已知事件不再被 generic posteventnext 提前吞掉；赛事结果关闭后继续驱动队列，队列空才会议。
- 星级庆典确认后回到日报展示，再从日报关闭进入 post-report；邀请接受/拒绝和比赛结果均严格消费当前事件。
- post-report 最终 P0：星级庆典结束通过共享回调保留 stat 并重新展示日报，日报确认后才驱动事件队列；五星终局按钮改为专用 `finalenext`，仅当前 `five-star-finale` 事件可确认。
- `closeBusiness()` 按本日报告 token 幂等排入未完成赛事和五星庆典事件；赛事胜利不再旁路触发五星，五星仅由正式认证条件产生。共享 `drivePostReportEvents()` 按事件 kind 打开专用 UI，队列逐项完成后唯一进入会议。
- 新增4项真实行为回归：庆典→日报→post 顺序、赛事跨日队列/token/重复收盘、五星 finale 条件与幂等、专用驱动器与错误 kind 拒绝。定向 `tests/new-campaign.test.js` 25/25，全量 `npm test` 312/312，`npm run check` 全绿；性能基准和最终浏览器长流程仍待 Sol 发布验收。
- 会议/夜间/营业床/天明子包：会议改为稳定的经营/团队/世界三类议题目录，首会固定客人流失与人手不足，卡片显示员工态度、预期效果、逐人发言和实际效果，存档保留卡片与选择。
- 夜间员工行动接入现有自由活动移动：每人2次，完成动作后才扣次数；同事聊天双方扣次，员工主动找店主支持立即/稍后队列/婉拒且全店最多3人，店主主动交谈额度独立按员工记录并可存读。
- 营业床新增管理模式寻路/床边任务入口，完成后体力100、压力-20、士气不变，中断无收益；夜床确认增加床边确认门槛，夜→昼通过可注入转场显示“休息结束 经营时段”后才进入下一天。
- 聊天与夜间剧情 AI 失败自动写入本地回复；新增会议绑定议题 ID 校验与 `requestGameAILocalFallback()`，未知 ID、拒绝和无效响应均不阻塞流程。
- 新增会议、夜间三员工额度、营业床完成/中断、天明转场和 AI fallback 行为测试。当前定向 `tests/new-campaign.test.js` 31/31、全量 `npm test` 318/318，`npm run check` 全绿；仍未执行性能基准及最终浏览器验收。
- 普通日会议议题会根据日报评分和员工体力/压力标记紧急状态，仍只使用本地固定机械效果。
- 会议 production 收尾：新增 `meeting-controller.js` 共享 begin/close/AI context/ID 校验；普通日议题基于日报流失、评分、员工状态和世界驻留确定性生成，世界议题实际写入 `worldKnowledge`，效果写入 `effectApplied` 防重复。
- `openMeeting` 现在展示上下文、员工态度和 AI/本地发言，启动有限的 meeting AI 润色；AI 只允许返回当前卡片 ID 与 line，未知 ID、解析失败或不可用时自动回退本地台词。开会、选题、提前确认、进入夜间均调用存档。
- 新增动态议题、提前散会确认/取消、世界效果幂等、AI ID 回退和 save-load 稳定性行为测试。定向 `tests/new-campaign.test.js` 34/34，全量 `npm test` 321/321；直接 Node 语法检查通过。npm 初次检查受 C: 盘零空间影响，改用 G 盘缓存后完成最终 npm 验证。
- 会议 modal P0：主会议改为不可关闭且无 X/ESC 旁路；一选后结束会打开不可误触的确认框，“返回会议”专用 action 会恢复相同 cards/AP/selected，确认散会才进入 night；两选后正常结束。UI 选卡统一调用 `meetingAction`。
- 新增真实 UI handler 测试覆盖主 modal 锁定、提前散会返回和确认进入夜间；定向测试更新为 35/35，全量 `npm test` 更新为 322/322。
- 房间建造/移动 production 接线：建房在扣款前构造完整候选并自动附加门厅前台或员工房床；房间移动在 clone 上同步房内家具/污渍并校验根连通、门洞、重叠和邻接，失败保持布局与金币不变，内置家具绑定房间不变。
- 新增 9 项房间建造与移动行为测试，覆盖合法平移/旋转、断门网、重叠、休息室走廊、自动内置家具及失败原子性；定向 `node --test tests/new-campaign.test.js` 76/76、全量 `npm test` 363/363 全绿。`npm run check` 与版本记录校验通过；性能基准和最终浏览器长流程仍未验收。
- 房间拆除与家具移动 production 接线：拆房先 clone 移除房内全部家具/污渍并执行完整 validator，成功后才清理关联任务和退款；退款排除 builtIn 且读取真实 purchasePrice。家具拖动/旋转同样先验证 clone，普通家具按适用房间移动，内置家具限制在 boundRoom 内。
- 新增 8 项拆房/家具移动行为测试，覆盖根房保护、断连、普通家具移除、内置零价、家具重叠/越墙/旋转和绑定房限制；定向 `node --test tests/new-campaign.test.js` 84/84、全量 `npm test` 371/371 全绿。性能基准和最终浏览器长流程仍未验收。
- 步骤 3a P0 收尾：`Tavern.serialize()` 改为深拷贝快照并包含门洞快照；休息室必须直接连接可用走廊（legacy 仅保留既有布局例外，新增/移动仍校验）；房间蓝图排除内置家具并保留普通家具相对坐标/方向/品质/购买价；房间实例记录实际建造价，拆除退款按房间与普通家具 purchasePrice 的70%计算。
- 新增 8 项快照、走廊、蓝图和成本行为测试；定向 `node --test tests/new-campaign.test.js` 92/92、全量 `npm test` 379/379 全绿。性能基准和最终浏览器长流程仍未验收。
- 步骤 3a 最终 P0：拆房退款改用房间实际 purchasePrice；旧/新房间蓝图读取时按目标蓝图内置契约归一并过滤无标志旧内置家具，普通家具保留实际 purchasePrice；复制房间现在将自动内置家具与归一后的普通模板件组成一个完整 candidate，统一验证后才确认和提交；普通家具建造记录实际支付价。
- 新增 4 项真实候选/成本回归测试，定向 `node --test tests/new-campaign.test.js` 96/96、全量 `npm test` 383/383 全绿。性能基准和最终浏览器长流程仍未验收。
- 步骤 3b 批量替换/恢复 production：整店布局套用、建造撤销/重做、开局布局和存档恢复均先 clone/normalize 并执行 trusted mode 完整 validator，合法后一次原子替换；非法布局不会改动当前 Tavern、Sim、金币或选择。Game.loadFrom 失败时恢复旧运行态，openDay 对当前布局执行严格校验，legacy/tutorial 根模式不能互换。
- 新增 10 项整店导入、snapshot 恢复、开局布局、legacy 模式和 tampered openDay 行为测试；定向 `node --test tests/new-campaign.test.js` 106/106、全量 `npm test` 393/393 全绿。性能基准和最终浏览器长流程仍未验收。
- 步骤 3b 最终 P0：新增 Game.parseValidateSave production helper，主存档、备份、导入和恢复统一执行迁移、legacy seal/trusted mode 与 strict Tavern 校验；主档失败时只切换到 strict-valid backup，双方失败不覆盖当前运行态。newTavern 增加失败回滚，开局布局临时构造并校验后才写入运行态。
- 新增 6 项 save/import/backup/mode 原子性测试；定向 `node --test tests/new-campaign.test.js` 112/112、全量 `npm test` 399/399 全绿。性能基准和最终浏览器长流程仍未验收。
- 步骤 3b storage 最终 P0：抽出 `src/save-validation.js` 的 `parseValidateSave(input)`，统一支持 string/object、迁移、legacy seal、trusted mode 与 strict Tavern load；save、saveSlots、loadSlot、import、restoreBackup、export 全部复用。坏主档不会覆盖好备份，备份恢复只原子写目标主档，不改变当前运行态。
- 新增 8 项存档入口行为测试，定向 `node --test tests/new-campaign.test.js` 120/120、全量 `npm test` 407/407 全绿；`npm run check` 与版本校验通过。性能基准和最终浏览器长流程仍未验收。
- storage 最终入口 P0：`Game.loadFrom` 现在可直接消费一次性 validated result，复用 normalizedData/trustedMode/strictLoadedTavern；`saveToSlot(target)` 只有 strict 校验和写入成功后才切换 currentSlot/active；备份回退先写回主档，加载失败会恢复旧主档与运行态。
- 新增 4 项入口契约/备份原子性测试；全量 `node --test` 411/411 全绿，`node --check game.js` 与 `src/save-validation.js` 通过。版本校验已更新；npm wrapper 受 C 盘 ENOSPC 影响，未改变测试结果。
- quiet/corridor 子包：新增确定性 `roomNoise/roomQuietness` 与客房舒适 quiet 分项；员工休息室 quiet 不进入员工需求、床效或夜间恢复；维护费按走廊同面积同品质普通房的40%逐房取整并在日报提供分项；走廊污渍概率固定为普通房25%，保留走廊路径1.0与移动速度1.25。
- 新增 8 项 quiet/corridor 回归测试；全量 `node --test` 419/419 全绿，语法检查通过。性能基准和最终浏览器长流程仍未验收。
- 走廊污渍运行态修正：`Sim.tickWorld` 现在先按活动量选定实际房间，再将基础概率 `dt*traffic*0.0018` 交给 `dirtChanceForRoom`；因此走廊实际概率为 `0.00045`、普通房为 `0.0018`，固定阈值下分别验证为不生污渍与生污渍。
- 新增 1 项 `tickWorld` production 行为测试；定向 `node --test tests/new-campaign.test.js` 133/133、全量 `node --test` 420/420 全绿，`src/sim.js` 语法检查通过。性能基准和最终浏览器长流程仍未验收。
- UI/legacy 回归包：捏人控件统一 `button[type=button]` 并阻止属性点击提交/冒泡；候选、员工、客人详情和底部信息统一在姓名旁显示性别小字；内置床/前台/会议桌隐藏于普通家具目录，已放置内置家具的复制/升级/出售入口禁用；招聘界面预判员工房、会议座位、12人上限和入职费；legacy 世界界面按星级解锁，tutorial 按日期解锁；解雇清理员工任务、卧室与夜间交互队列。
- 新增 6 项 UI/legacy/行为回归测试，定向 `node --test tests/ui-interaction.test.js` 26/26、全量 `npm test` 426/426 全绿；`npm run check` 与版本校验通过。响应式最终浏览器截图仍待验收，性能基准仍未验收。
- 浏览器验收尝试启动 Playwright Chromium 时因 C 盘剩余空间不足（ENOSPC，浏览器约需192MiB）未能生成截图；代码级 1280/1366 响应式边界规则与现有 CSS 回归测试保留，需在有浏览器运行时的环境补跑。
- UI 包 P0/P1 收尾：新增 `releaseStaffWork(staffId, reason)` 生产释放器，解雇时清理任务/工位/夜间队列，并将备餐/烹饪订单恢复为可续接状态、保留已扣原料标志；上菜搬运中断时恢复出餐盘，避免重复扣库存或收费。legacy 允许补购旧档缺失 desk/bunk，但会议桌仍不进目录；招聘预判仅对 legacy 豁免会议座位，仍检查员工房、12人上限、候选与费用；统一 `renderPersonName` 覆盖交互和 AI 对话标题；积压工作时空闲员工不集体休息。
- 新增 6 项解雇、legacy 目录、招聘门控、姓名渲染和优先级行为测试；全量 `npm test` 432/432 全绿，`npm run check` 与版本校验通过。性能基准和浏览器截图仍未验收。
- UI 包最终 P0：解雇上菜任务改读取订单 `servicePicked` 并在释放后清零，精确恢复正确出餐台且幂等；交互、员工详情、背景、AI 对话和夜间主动提示统一使用 `renderPersonName`；捏人选项抽出生产 dispatcher 并由测试注入 preventDefault/stopPropagation；优先级真实机械链验证存档恢复与任务匹配，紧急积压时至少保留任务分配而不集体休息。
- 新增 2 项真实生产链测试；全量 `npm test` 434/434 全绿，`npm run check` 与版本校验通过。性能基准和浏览器截图仍未验收。
- UI 包最终行为补测：卧室挂名也统一通过 `renderPersonName` 保持性别小字紧邻；新增真实营业 tick 场景，验证两名低体力员工面对待处理迎宾任务时至少一人接单、不会集体进入休息。定向 UI 测试 35/35、全量 `npm test` 435/435、`npm run check` 与版本校验全绿；性能基准和浏览器截图仍未验收。
- UI 最终 P1：聊天历史、关系同事、能力/状态说明、夜间队列、房间入住与驻守等可见角色姓名统一接入 `renderPersonName`（原生 option 使用姓名·性别紧邻文本）；新增 `handlePriorityUiAction` 作为 UI/Game/测试共享职责优先级入口，真实验证 setter 调用与姓名视图性别标记。定向 UI 测试 37/37、全量 `npm test` 437/437、`npm run check` 与版本校验全绿。
- UI 姓名视图收尾：进修归来、AI 进修反思卡与店主身份页改用 `renderPersonName`，性别标记保持紧邻且不再以同字号纯文本分列；新增三处生产模板断言，定向 UI 测试 38/38、全量 `npm test` 438/438、`npm run check` 与版本校验全绿。
- 性能专项第一阶段：Sim 任务计划增加脏原因、generation、最长 0.5 秒安全重扫和 rebuild instrumentation；订单/客组/释放/优先级状态变化主动失效。Tavern 增加房间/家具 ID 与类型索引，路径缓存改为真正 LRU；Game 增加角色/物品视口裁剪和 `window.__hotelPerf` 计数入口。新增 `tests/performance.test.js` 三项行为测试与固定 35 房/250 家具/12 员工/30 客 fixture harness，原始结果保存在 `artifacts/performance/`。当前 CPU harness 的同机 P95 结果会受进程调度影响，最新一次 5000 帧为 1x current 0.2134ms vs dist baseline 0.2037ms、4x current 0.2416ms vs 0.2356ms，尚未达到 30% 门槛，浏览器完整基准仍需继续优化/复测。
- 性能 fixture/浏览器基准补齐：生成固定 v10 legacy 存档 `artifacts/performance/fixture-v10.json`（SHA-256 `d0d7545e88c48185b451379705bfcecb27054a5539eb26ef5e2a9af221411b28`），current 与未改动 dist 均实际加载并核对 35 房、250 家具、12 员工、30 客。Edge `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`、NVIDIA RTX 2070 D3D11、1366×768/100% 下完成 1×/4×各5轮、每轮预热5秒采样10秒；current/dist 页面错误均为0。正式 P95：1× current 23.52ms、dist 17.74ms；4× current 23.50ms、dist 19.02ms（该固定旧版基准下 current 未达到30%改善门槛，数字与原始轮次见 `artifacts/performance/browser-raw.json`；`debug.json`保留GPU与2秒调试证据）。
- 基准脚本逐轮写入 `browser-progress.json`，每轮有180秒硬超时并在完成后释放 server/browser 句柄；软件渲染调试数据未计入正式结果。
- 性能子包 B：fixture 改为通过生产开门路径进入真实营业（`dayActive=true`、`phase=business`、35 房/250 家具/12 员工/30 客，current 首次任务计划分配6名员工）；补入门厅、厨房、储藏、客房、餐桌与基础生产线，保持250件家具精确数量。Game 记录实际 frame 时长/P95/长帧，UI 对 toast 和结构面板做签名脏更新并暴露 panel rebuild 计数。当前 active browser artifact 为 `artifacts/performance/browser-raw.json`，页面错误保持0；旧 dist 只能通过 DOM 营业态确认，current 记录完整 runtime counts/task instrumentation。
- 性能子包 B 正式结果：fixture 文件 SHA-256 `cac765e695e192247a812e8bc3d81f5608a787b6666dcd16192ba37e7a257a60`；current 每轮均确认 `dayActive=true/phase=business/groups=30/staff=12/rooms=35/furniture=250`，taskCount 为6或12，任务重建与 panel rebuild 均有记录。GPU Edge 1× current P95 11.90ms、dist 76.50ms（改善约84.4%）；4× current 12.00ms、dist 82.30ms（改善约85.4%）；20轮页面错误0。current 平均 FPS 为1×109.4、4×100.7；renderer 均为 NVIDIA RTX 2070 D3D11。结果未降低可见对象，视口裁剪只隐藏屏幕外对象。
- 任务事件补全：客人引座、需求推进和座位状态变更现在立即失效任务 generation；安全重扫仍保持最长0.5秒，避免稳定营业期间全量重建。
- 性能门修订：面板脏签名现在覆盖员工任务/需求/自由活动/职责、客群耐心/订单/设施/成员气泡、房间清洁/维护/安静度/噪声/舒适度、家具脏污/盘子/占用以及夜间队列；新增动态字段变化行为测试。任务安全重扫测试固定验证 0.49 秒不重建、跨 0.5 秒且 reason=safety 才重建；污渍签名使用排序后的完整坐标/等级键，避免加权碰撞；渲染视口每帧缓存一次，气泡/标签按同一视口边界裁剪。
- 性能脚本修订：固定 fixture 的 canonical SHA-256 为 `46931cc06cda33f50a5ce8ef583bf5588c77bc6cd9886dda80c0b98d388d6c00`，sidecar 与内嵌值同源；基准记录 current/dist 的 console.error、pageerror、requestfailed、运行环境、源码哈希、GPU renderer、P99/max 与交替 AB/BA 顺序。当前验证：`npm test` 443/443 全绿、`npm run check` 通过；正式20轮基准等待 Sol 放行，尚未以本次候选结果宣称性能门达标。
- 性能预审修订：overlay 标签/气泡统一把像素世界坐标转换为逻辑格坐标后再裁剪；Game 建造、移动、拆除、升级和 Sim 任务完成/失败/寻路失败均增加任务失效原因。面板签名补充员工好感。fixture 清除未定义过夜评分字段并补齐 hygiene/origin 等有限数值，重新生成 canonical SHA-256 `e85e2bd91438e68c9fe580b5e28283164c263e80957b5f3ba1f36b3d85f971fd`；current/dist 短调试均加载 35/250/12/30，current 约113帧/2秒、RAF P95 82.4ms，dist约49帧/2秒、RAF P95 129.2ms，评分均为有限值。全量 `npm test` 443/443、`npm run check` 通过；正式20轮仍等待 Sol 放行。
- 性能预审第二轮修订：员工岗位色标/进度环、客人耐心环/餐盘标、设施动画与 FX 现在都经过统一视口裁剪；fixture 运行态改为不冲突的 `wait/ordered/seated/eating/checkout/facility_prepare` 固定分布，queued 订单不再挂在 seated 客组，设施客仅保留一名并绑定唯一床位。浏览器短调试 current/dist 的状态直方图与任务直方图完全一致，均为 dayActive/business、35/250/12/30、有限数值且 frameErrors=0；记录 browser version、UA、DPR、visualViewport scale、zoom、P95/P99/max。`npm test` 443/443、`npm run check` 通过；正式20轮仍等待 Sol 放行。
- 基准失败路径现在也会关闭 Edge/browser/server 句柄；每轮 row error 和累计 page/console/request 错误均以非零失败，避免 undefined P95 被汇总。版本校验待本条记录落盘后复跑。
- 性能预审第三轮：抽出 setup 后/采样后的 `captureContract`，初始化严格锁定客群状态直方图 `{wait:12, ordered:4, seated:2, eating:8, checkout:3, facility_prepare:1}` 与任务直方图，采样后复验有限值、营业态、固定数量和持续负载；current/dist 的 setup 与 sample 归一快照严格比较。新增真实 Edge UI 测试，单独改变员工好感、房间清洁和夜间队列后调用 `render(false)`，分别验证 HTML 与 panel rebuild，静止状态不重建；新增共享 `worldPointVisible/pixelWorldVisible` 屏内/屏外 predicate 测试覆盖角色、物品、标签/气泡、状态环、设施动画和 FX。`npm test` 446/446、`npm run check` 通过。
- 性能预审失败修复（仅 fixture/合同）：统一固定 v10 fixture 的 canonical SHA-256 为 `f7913e461e250cfda9bc3ad8f038c4aabf37fc4a71c6670095e203ecf0be9972`，为客人补齐 `mood/aff/seatId/aiChatLog/bubble` 等运行态有限字段，消除真实推进中的 NaN。采样合同允许两版本自然推进 seating/checkout 等状态，但仍硬性要求 dayActive/business、35 房/250 家具/12 员工/30 客、全部 finite、非全离场且任务持续有效；setup 直方图仍严格固定，current/dist 采样只比较明确语义合同。`PERF_DEBUG=1 node scripts/performance-browser.mjs` 完成 current 与 dist 各一轮、预热1秒/采样10秒：两页 GPU 均为 NVIDIA RTX 2070 D3D11，约 56–61 FPS，current RAF P95 82.4ms、dist 82.4ms，均无 page/console/request/frame 错误；运行时合同通过。`npm test` 446/446、`npm run check` 通过。正式20轮未重跑，等待 Sol 再次放行。
- 短调试结果以最新 artifact 为准：current 581 frames/58.1 FPS、RAF P95 82.0ms/P99 152.9ms/max 294.1ms；dist 507 frames/50.7 FPS、P95 88.2ms/P99 135.4ms/max 282.4ms；两页 page/console/request/frame errors 均为0，sampleMs 元数据为10000。该单轮短样本不是正式性能门结果。
- 性能合同唯一阻断修复：采样归一快照现在包含完整 `groupStates`、`taskCount` 与排序后的任务种类集合；两版本允许任务在不同员工间有单项分配差异，但要求总任务数、任务种类和完整客群状态一致。持续负载门槛改为非离场客至少8、任务至少6，避免以不存在的 wait 状态误判。语法、性能定向3/3及 current/dist 各10秒短调试通过；最新短调试 page/console/request/frame errors 均为0，fixture SHA 保持 `f7913e461e250cfda9bc3ad8f038c4aabf37fc4a71c6670095e203ecf0be9972`。正式20轮仍等待 Sol 放行。
- benchmark 镜头与清理收尾：性能短调试固定 zoom=1、镜头中心 `(10,3)` 覆盖中央餐饮区，保持真实角色/物品视口裁剪和 35/250/12/30 负载合同；harness 在结束阶段显式清理浏览器与 HTTP 子进程，记录环境和可见负载，不影响生产源码或 dist。短调试结果与正式门分开记录，正式20轮仍需单独放行。
- benchmark cleanup 验证：harness 结束时终止并等待 HTTP 子进程、清理 Edge 进程句柄并显式传播 `EXIT=0`；单次 `PERF_DEBUG=1` current/dist 各10秒通过，current 25 actors/4 items、P95 76.5ms，dist 30 actors/24 items、P95 82.4ms，有限值、营业态和错误合同均通过；无对应 benchmark Node 或 4173 服务残留。harness SHA-256 `35cd515458384c4d4597391866e4d823dbd704bc92fb8f289bce4e7d6ee48632`。
- cleanup 最终修订：`closeBrowser` 保存 active browser 后立即清空共享引用，只对 `active.close()` 做5秒有界等待，不调用不存在的 browser process API；短调试明确输出 `EXIT=0`。current/dist 仍通过有限值、营业态、可见负载和错误合同，harness SHA-256 更新为 `933dd1faf08d851888cedc7fcf50ab28f0fc0b75cc8ed8bdf6fb26a8ad935863`，复查无 headless Edge、benchmark Node 或4173服务残留。
- 教程 UI P0：建房教学目标现在按房间 `kind` 匹配蓝图卡（首员招募阶段仍按具体 lounge blueprint id），因此位面门厅目标卡真实带有 `data-act=bp` 且可点击，非目标卡继续 aria-disabled；Game 方法层门控未放宽。新增生产共享判定回归，UI 定向测试 40/40；node 语法检查通过。npm wrapper 再次受 C 盘 ENOSPC 影响，未据此宣称全量 npm 通过。
- 教学家具完成 P0：`applyTutorialFurniturePlaced` 现在以当前 Tavern 中真实存在的家具补足 `furn_*` 教学状态，初始玩家休息室椅子即使没有历史 flag 也会被计入；完成 desk/table/chair/prep/stove/pass/sink/shelf/bed 后幂等进入 `ready-open`，`canOpenBusinessNow()` 与顶部开门入口恢复。新增标准新档真实 Edge 链：foyer 目标卡 `data-act=bp/data-v=foyer4` 可点击、非目标卡禁用；全量 `npm test` 449/449、`npm run check` 与版本校验通过。
- 教学存档恢复 P0：resume/render 教学时保留房间目标卡的 `tutorial-target` 高亮，避免重新渲染时清掉真实可点击目标；新增真实布局同步器，在载入/恢复时把缺失 `furn_chair` flag 但家具齐全的半完成档幂等修复为 `ready-open`，并对无 `nextFurniture` 的标题渲染做防抛错保护。标准新档、序列化/恢复链和开门门控回归通过，`npm test` 450/450、`npm run check` 与版本校验通过。
- 教学餐桌座位 P0：教学家具共享判定现在要求普通 `table` 位于餐厅且至少有一把朝向有效的餐厅椅，玩家休息室会议桌椅不会满足餐饮座位；`startBuildFurn`、画布选房、UI 下一件提示、`openDay` 和 `Sim.canOpenBusinessNow` 共用该判定。缺有效餐椅的 `ready-open` 存档会恢复到 `tutorial-furnish` 并重新引导摆椅；新增真实座位与恢复回归，`npm test` 452/452、`npm run check` 通过。
- 教学家具 UI 回归：家具卡片列表改用同一餐厅有效座位 predicate，初始会议椅存在时仍正确高亮餐厅椅卡（`data-act=furn`、非禁用），其它卡继续禁用；新增真实 Edge DOM 点击测试，定向测试通过。
- 教学首日客流 P0：教学第1—3日关闭随机经营/世界事件窗口与动态事件回放；住宿脚本客不再随机绕去餐饮设施，固定波次保持其 `meal/sleep` 需求。新增真实开门后推进测试，首波固定为1位餐饮客、无 notable、无随机事件弹窗。
- 首日赠礼流程 P0：赠礼确认不再被通用空队列逻辑直接推进到会议；保持 `first-recruitment`，允许玩家离开弹窗建员工休息室、补会议椅并完成首员入场后才进入会议。赠礼确认不重复发放已入账的1000界币；新增真实 UI handler 到首员确认链回归。
- 夜间管理回床 P0：抽出不依赖渲染的生产回床 controller，Game 每帧会推进管理模式店主从会议椅到玩家床边，解除坐姿并触发结束日确认；修复缺失外观身高字段导致 staffSpeed 为 NaN、路径永不移动的问题。直控路径保持不变，新增跨帧回床/确认回归。
- Day1 波次时序 P0：固定教学波次现在等待上一批真实完成（餐饮离店、住宿客到达床位）后才放下一批，避免4倍速下多批重叠造成耐心耗尽；d1-loss 仍只在床教学期间释放，随机事件屏蔽保持不变。
- Day1 首批营业闭环：店主在非直控营业时不再受默认 greeter 岗位限制，可接手备餐、烹饪、出餐、结账与清洁；真实 Sim.update 回归确认首批餐饮完成并进入 d1-lodging，未发生非教学流失。新增 1 项真实营业链测试；当前定向 `node --test tests/new-campaign.test.js` 141/141 通过。
- 教学挑战时序修复：教程第1—3日屏蔽常规客人挑战，仅允许 `d1-taste` 第三批两人餐饮客触发固定口味挑战；首批餐饮与住宿批次不会生成普通 challenge。新增生产时序测试，定向测试 142/142 通过。
- 首日第三批容量与教学门控：餐厅教学现在要求同一普通餐桌具备至少两把有效餐椅，确保两位 `d1-taste` 客人可实际入座；会议椅不计入。全量 `npm test` 458/458 通过。
- 首员招募路径修复：首日赠礼后的 `first-recruitment` 阶段先精确高亮并允许建造 `corridor2`，再允许建造 `lounge5`；保留全局员工房必须连接走廊的空间规则，完成走廊→员工房→第二把会议椅后才开放招聘。生产门控与共享流程回归通过。
- 首日床教学入口：首批餐饮结账后设置可存档的 `bedPrompt`，营业教程在画布上高亮玩家床并说明管理点击/直控 E 两种操作；管理点床会取消当前普通任务后寻路到床边启动休息，完成后关闭提示并允许固定 `d1-loss` 波次按教学时序生成。
- 床教学接线与挑战幂等：`day1-open` 与 `business` 均使用同一生产 UI 步骤，床提示明确管理点击/直控 E；教程第1—3日完全屏蔽普通 facility challenge，`d1-taste` 仅保留一张固定口味卡。新增 UI/强制 RNG 回归测试。
- 走廊蓝图精确化：首员教学固定使用 `corridor2`（2×3、20界币），不再因蓝图顺序误选长走廊；UI target、Game 方法门控和共享判定统一按显式 id。
- 床教学时序收紧：`bedPrompt` 改为固定口味挑战确认后才出现，而非首批结账时；新增真实断言 `d1-loss` 在店主 rest 任务尚未完成时离场，随后才结算床教学完成。
- 夜间交谈与首员入场 UI 收口：夜间最后一条“待回应”处理成功后立即关闭旧交谈弹窗并刷新底栏，队列未空时只打开下一条；首员雇用成功即自动弹出明确的“新同伴入场”介绍，不再依赖被最小化的旧版“继续引导 1/18”卡片。新增两项生产 `UI.onClick` 行为回归；本轮定向 `tests/new-campaign.test.js` 147/147 通过。
- 入场确认按钮改用专用 `intro-confirm-action` 样式类，避免被主经营界面的单一 `primary-action` 兼容断言误计为第二个主操作；相关旧 UI 断言保持通过。
- 教学时间控制回归：`day1-open` 尚未出现床提示、剧情卡暂时隐藏时，暂停与 1×/2×/4× 倍速仍可正常响应；其它第一天功能门控保持不变。新增生产 `UI.onClick` 回归。
- 固定口味挑战收尾修复：关闭 `d1-taste` 结果卡后自动激活并渲染床教学，隐藏旧 17 步“继续引导”入口，避免 `d1-loss` 因玩家误走旧教程而一直等待。新增结果卡关闭后的生产 UI 回归。
- 首员与四日数据口径收口：赠礼确认后自动激活 `first-recruitment` 教学卡；餐饮→住宿组合客按每日唯一访客计入接待人数/世界日报，服务次数仍保留在订单与收入记录；第3日两名 `d3-sect` 成员都固定为“同门”并共享身份字段。新增 UI、Day2 组合客和 Day3 成员快照回归。
- 第3日迁界过场修复：首次免费迁往玄黄界现在显示带中文标题的全屏遮罩至少约700ms，完成后自动衔接营业，不再出现30ms闪过后还需再次点击开门；生产波次测试继续验证迁移前后世界状态。
- 响应式顶栏收口：1280×720 与 1366×768 桌面宽度下压缩日期、倍速、经济和操作组的占用，保证档位与“更多”按钮实际落在 viewport 内；Edge DOM 回归现在逐个检查可见顶栏按钮边界与可达性。
- legacy 夜床兼容：v10 旧档没有玩家休息室时，店主夜间结束日改用合法且空闲的员工休息室床位，管理/直控路径共用该床位选择；底栏与提示改为“员工休息室床边”。新增 legacy 夜床生产回归。
- 本轮验证收口：legacy 夜床、唯一访客统计、Day3 同门成员、免费迁界转场和 1280/1366 顶栏边界回归均通过；`tests/new-campaign.test.js` 152/152、`tests/ui-panel-render.test.js` 3/3，`npm run check` 通过。
- 性能合同公平性修复：固定 fixture 初始化状态与任务直方图仍要求 current/dist 完全一致；采样阶段改比较稳定营业负载合同（营业态、30客/12员工/35房/250家具、finite、非离场下限、任务数量下限），允许真实调度造成的座位/结账/任务类别先后差异，不把逐帧自然演化误判为语义不等价。
- 性能短调试验证：current/dist 各完成 1×、10 秒采样，初始化合同严格一致，采样均保持 30 客/12 员工/35 房/250 家具、finite、非离场负载与任务下限，page/console/request/frame errors 为 0；current 1× P95 135.1ms、dist 1× P95 134.7ms。正式 20 轮未启动，等待 Sol 冻结新脚本哈希后放行。
- 性能采样配对合同再收紧：current/dist 采样只比较非离场负载是否达到 `>=8` 的 `loadFloor` 布尔值，不比较自然演化中的精确人数；每页仍独立硬校验 30 客、允许状态集合、非离场≥8、任务≥6、finite 与错误为0。正式20轮未启动。
- 性能合同短调试复核：harness SHA-256 `9e52c80bbf0adf3b081dc30544bd5631c2a098434dc3c3b53e413a75ea60d346`；current/dist 1×各完成10秒，`loadFloor`/`taskFloor`及固定 setup 合同通过，page/console/request/frame errors 为0。正式20轮仍未启动。
- 性能可见负载门修复：采样期逐帧记录角色/物品可见数 min/avg/max，硬门改为角色平均可见≥5、物品平均可见≥1并保留极值证据；不再因单帧角色裁剪瞬时低于5误判，业务与初始化合同不变。正式20轮未启动。
- 性能短调试口径：调试模式覆盖1×与4×，逐页保留可见角色/物品 min/avg/max，并以采样平均角色≥5、物品≥1作为可见负载门；正式20轮仍未启动。
- 性能短调试实测（Edge 134、RTX 2070 D3D11、1366×768、DPR/缩放1）：current 1× P95 200.0ms、4× 202.5ms，角色可见25/25/25、物品4/4/4；dist 1× P95 187.5ms、4× 247.4ms，角色30/30/30、物品24/24/24。四行 errors=0、finite=true、营业态与30客/12员工/35房/250家具合同通过；harness SHA-256 `a8d35501e9337e5ccc6cb950c4b62c07c77c6f1c16cb93a2998f620189c0faa8`。正式20轮未启动。
- 正式性能发布门（冻结 harness SHA-256 `a8d35501e9337e5ccc6cb950c4b62c07c77c6f1c16cb93a2998f620189c0faa8`，fixture canonical SHA-256 `f7913e461e250cfda9bc3ad8f038c4aabf37fc4a71c6670095e203ecf0be9972`）：GPU Edge 134、RTX 2070 D3D11、1366×768、DPR/缩放1，1×与4×各按 current/dist 交替完成5轮，每页预热5秒、采样10秒，共20/20轮。1×五轮中位 P95 为 current 47.10ms、dist 82.40ms，改善42.84%；4×为 current 52.90ms、dist 88.20ms，改善40.02%，两档均超过30%门槛。current平均可见负载约24.3角色/4物品，dist为30角色/1×24物品、4×21.37物品；全程保持营业态、35房/250家具/12员工/30客、finite、非离场与任务下限，page/console/request/frame errors均为0。原始20轮与冻结源码哈希记录于 `artifacts/performance/browser-raw.json`。
